Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
## What's Changed

### 🔧 Improvements
- Your connection choice is now remembered between sessions, and the plugin waits until the selection is settled before displaying it, so you no longer see the wrong connection flash on screen
